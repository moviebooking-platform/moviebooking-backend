// M2: Identity Service enums
export enum UserStatus {
  ACTIVE = 'ACTIVE',
  DISABLED = 'DISABLED',
}

// M3: Theatre Service enums
export enum TheatreStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
}

export enum TheatreAdminStatus {
  ACTIVE = 'ACTIVE',
  DISABLED = 'DISABLED',
}

export enum ScreenStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
}

export enum SeatType {
  STANDARD = 'STANDARD',
  VIP = 'VIP',
}

export enum SeatStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
}
